export type DataType = {
  _id: string;
  email: string;
  firstName: string;
  lastName: string;
  address: string;
  entryDate: string;
};

export type ObjKeyType = {
  id: string;
  email: string;
};
